"use client";

import type { Post } from "@/types/jokes";

export default function PostMedia({ post, active }: { post: Post; active: boolean }) {
  // Your backend does not provide media fields yet.
  // So this component should render nothing for now.
  return null;
}

