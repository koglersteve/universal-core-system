"use client";

export default function AdBanner({ position }: { position: "top" | "inline" }) {
  return (
    <div className="w-full bg-white/10 text-white text-sm p-2 text-center">
      {/* Ad content */}
    </div>
  );
}
