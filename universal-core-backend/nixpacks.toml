[phases.setup]
nixPkgs = ["nodejs_latest", "openssl"]

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "node dist/index.js"
