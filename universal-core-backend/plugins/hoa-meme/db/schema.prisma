datasource db {
  provider = "sqlite"
  url      = "file:./meme.db"
}

generator client {
  provider = "prisma-client-js"
}

model Meme {
  id        Int      @id @default(autoincrement())
  userId    String
  imageUrl  String
  template  String?
  title     String?
  createdAt DateTime @default(now())
}

